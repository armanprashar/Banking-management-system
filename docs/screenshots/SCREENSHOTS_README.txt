Screenshots (portfolio assets)

Capture these after running the app against a populated MySQL schema:

1. Login screen — branding + Register / Forgot password.
2. Customer dashboard — Accounts grid + Open account.
3. Transactions tab — filters, double-click receipt dialog.
4. Banking tab — deposit / withdraw / transfer success dialog.
5. Admin dashboard — Customers + freeze toggles, Transaction log CSV export.
6. Dark theme toggle — same view in alternate theme.

Save PNG files here as login.png, customer-dashboard.png, admin-dashboard.png, dark-theme.png.
